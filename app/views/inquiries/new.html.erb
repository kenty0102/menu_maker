<% content_for(:title, t('.title')) %>
<div class="container">
  <div class="col d-flex justify-content-end align-items-center">
    <span class="d-inline-block my-3">
      <%= link_to t('.to_top_page'), root_path, class: "custom-link", data: { turbo: false } %>
    </span>
  </div>
  <h1 class="page-title text-center"><%= t('.title') %></h1>
  <%= form_with model: @inquiry, url: inquiries_path, method: :post, data: { inquiry_confirm: t('.send_inquiry_confirm') } do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
    <div class="card custom-card-width">
      <div class="container py-2 my-5">
        <div class="row justify-content-center">
          <div class="col-md-8">
            <div class="mb-3">
              <%= f.label :name, class: 'form-label' %>
              <%= f.text_field :name, class: "form-control" %>
            </div>
            <div class="mb-3">
              <%= f.label :email, class: "form-label" %>
              <%= f.email_field :email, class: "form-control" %>
            </div>
            <div class="mb-3">
              <%= f.label :message, class: "form-label" %>
              <%= f.text_area :message, class: "form-control", rows: 5  %>
            </div>
          </div>
        </div>
      </div>
    </div>
    <div class="row my-5">
      <div class="col d-flex justify-content-center align-items-center">
        <%= f.submit t('.send_inquiry'), class: "btn btn-primary", data: { turbo: false } %>
      </div>
    </div>
  <% end %>
</div>
